spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zhuanzhuan?useUnicode=true&characterEncoding=utf8mb4&serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: root123

  data:
    redis:
      host: localhost
      port: 6379
      password: redis123

  mail:
    host: ${MAIL_SERVER}
    port: ${MAIL_PORT}
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    default-encoding: UTF-8
    properties:
      mail.smtp.auth: true
      mail.smtp.ssl.enable: ${MAIL_USE_SSL}
      mail.smtp.starttls.enable: ${MAIL_USE_TLS}
      mail.smtp.ssl.trust: ${MAIL_SERVER}

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
